extends master.pug

block body
  #root.is-fullscreen
    .app-error
      a(href=siteConfig.basePath || '/')
        img(src=siteConfig.assetBasePath + '/svg/logo-wikijs.svg')
      strong Oops, something went wrong...
      span= message
            
      if error.stack
        pre: code #{error.stack}
